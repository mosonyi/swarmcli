name: License checks

on:
  pull_request:
    branches: [ main ]
  push:
    branches: [ main ]

jobs:
  spdx:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check SPDX headers
        run: |
          ./scripts/check-spdx.sh
